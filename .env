# ── Postgres ──────────────────────────────────────────────────────────────────
POSTGRES_DB=orders_db
POSTGRES_USER=orders_user
POSTGRES_PASSWORD=orders_pass

# ── Django ────────────────────────────────────────────────────────────────────
DJANGO_SECRET_KEY=i1m43vgdzdcv3yo3765yldf6f69xcz0bvhrsd54q8buhvpy1tu
DEBUG=False
ALLOWED_HOSTS=localhost,yourdomain.com

# ── CORS ──────────────────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost,https://yourdomain.com

# ── Vite (frontend) ───────────────────────────────────────────────────────────
# The frontend calls /api through nginx, so the URL is just the origin.
# In docker the nginx proxies /api → backend, so this must point to the nginx host.
VITE_API_URL=http://localhost/api
